import React, {Component} from 'react';
import {BrowserRouter as Router, Route} from 'react-router-dom';
import Data from './dataDummy'

export default class BookDetil extends Component {
    constructor(props) {
        super(props)
        Data
    }
    render() {
        return (
            <div>
                
            </div>
        )
    }
}
